services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: life_timer
      POSTGRES_PASSWORD: life_timer
      POSTGRES_DB: life_timer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: