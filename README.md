# 🕐 Life Timer

Персональный таймер-планировщик дня с геймификацией.  
Принцип: **часы идут всегда**. Нет активной задачи — время уходит в прокрастинацию.

![Python](https://img.shields.io/badge/Python-3.10+-blue)
![Platform](https://img.shields.io/badge/Platform-Windows-lightgrey)
![License](https://img.shields.io/badge/License-MIT-green)

---

## ✨ Возможности

- **Шахматный таймер** — один счётчик на весь день, 24 часа суммируются
- **Прокрастинация** — остаток = время до полуночи минус незавершённые задачи
- **Приоритеты задач** — 🔴 высокий (×2 монеты), 🟡 обычный, 🔵 низкий (без монет)
- **Геймификация** — монеты за выполнение задач, серии дней, множитель
- **Магазин наград** — трать монеты на реальные вознаграждения себе
- **Шаблоны** — сохраняй типовые наборы задач и применяй одним кликом
- **Перенос задач** — незавершённые задачи переносятся на следующий день
- **Статистика** — история по дням, монеты, продуктивность

---

## 🚀 Установка (для пользователей)

### Вариант 1 — готовый exe (рекомендуется)

1. Скачай последний релиз: [Releases →](../../releases)
2. Распакуй архив в любую папку
3. Запусти `Life Timer.exe`

> ⚠️ Антивирус может предупредить о незнакомом приложении — это нормально  
> для exe собранных PyInstaller без подписи. Добавь в исключения.

База данных (`life_timer.db`) создаётся рядом с exe при первом запуске.  
**Не удаляй её** — там все твои данные.

### Вариант 2 — из исходников

**Требования:** Python 3.10+

```bash
git clone https://github.com/ВАШ_НИК/life-timer.git
cd life-timer
pip install -r requirements.txt
python main.py
```

---

## 🔨 Сборка exe (для разработчиков)

```bash
# Просто запусти скрипт — он сам установит зависимости и соберёт
build.bat
```

Результат — папка `dist\Life Timer\`. Заархивируй и выкладывай на GitHub Releases.

**Вручную:**
```bash
pip install pyinstaller
pyinstaller life_timer.spec --noconfirm
```

### Добавление иконки

Положи `icon.ico` в папку `assets/` перед сборкой.  
Без иконки сборка всё равно работает — PyInstaller использует дефолтную.

---

## 📁 Структура проекта

```
life-timer/
├── main.py              # Точка входа, главное окно
├── timer.py             # Движок таймера (поток)
├── gamification.py      # Логика монет и серий
├── repository.py        # Работа с БД
├── adapter.py           # DTO / модели UI
├── lt_db.py             # SQLAlchemy модели и миграции
├── validation.py        # Валидация плана дня
├── ui/
│   ├── timer_header.py  # Верхняя панель
│   ├── task_panel.py    # Список задач
│   ├── shop_dialog.py   # Магазин наград
│   ├── stats_panel.py   # Статистика
│   ├── settings_dialog.py
│   ├── tooltip.py       # Тултипы и подсказки
│   └── ...
├── tests/               # Юнит-тесты
├── life_timer.spec      # PyInstaller конфиг
├── build.bat            # Скрипт сборки (Windows)
└── requirements.txt
```

---

## 💰 Система монет

| Приоритет | Ставка | Штраф за скип |
|-----------|--------|---------------|
| 🔴 Высокий | 1 🪙 / 5 мин | = базовая награда |
| 🟡 Обычный | 1 🪙 / 10 мин | = базовая награда |
| 🔵 Низкий  | 0 🪙 | 0 |

**Множитель серии:**
- 1 день → ×1.1
- 5 дней → ×1.5  
- 10+ дней → ×2.0

Монеты начисляются в **полночь** по итогам дня.  
Трать в **Магазине** на реальные награды которые сам настраиваешь.

---

## 🧪 Тесты

```bash
python -m pytest tests/ -v
```

---

## 📝 Лицензия

MIT — делай что хочешь, ссылка на автора приветствуется.

---

## 🤖 Автоматические релизы (GitHub Actions)

При пуше тега релиз собирается автоматически для Windows и macOS:

```bash
git tag v1.0
git push origin v1.0
```

GitHub Actions запустит сборку на обеих платформах и создаст релиз  
с двумя артефактами: `Life-Timer-windows.zip` и `Life-Timer-macos.zip`.

### Нюансы macOS

- Иконка должна быть в формате `.icns` (не `.ico`) — положи в `assets/icon.icns`
- При первом запуске Gatekeeper ругается на неподписанное приложение:  
  **ПКМ → Открыть → Открыть** (один раз, потом запускается нормально)
- Для полноценной подписи нужен Apple Developer аккаунт ($99/год) —  
  для личного использования не обязательно
