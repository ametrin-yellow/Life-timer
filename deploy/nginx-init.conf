server {
    listen 80;
    server_name timer-of-life.com www.timer-of-life.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Life Timer — setting up HTTPS...';
        add_header Content-Type text/plain;
    }
}
